# USB Insight AI Releases

This public repository contains only Windows installer binaries and safe release metadata for USB Insight AI.

It intentionally does **not** contain the private application source code, API keys, databases, logs, user files, or user paths.

Use the latest GitHub Release asset to install or update the application. The app reads `latest.json` over HTTPS only to offer an optional update download; offline local USB workflows remain available.